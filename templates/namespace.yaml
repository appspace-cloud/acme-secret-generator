{{- if .Values.createNamespace }}
---
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.namespace | default "acme-secret-generator" }}
  labels:
    {{- include "acme-secret-generator.labels" . | nindent 4 }}
{{- end }}

